{
  "license": "GPL-3.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "postinstall": "lerna run postinstall",
    "build": "lerna run build",
    "start-client": "npm-run-all start --prefix packages/client",
    "start-server": "npm-run-all start --prefix packages/server",
    "prerelease": "npm-run-all build test",
    "release-alpha": "npm-run-all release -- preversion --preid alpha",
    "release-beta": "npm-run-all release -- preversion --preid beta",
    "release-rc": "npm-run-all release -- preversion --preid rc",
    "release": "lerna version",
    "publish": "lerna run publish",
    "license-check": "lerna run license-check"
  },
  "devDependencies": {
    "add-cors-to-couchdb": "^0.0.6",
    "jasmine": "^3.2.0",
    "lerna": "^3.4.0",
    "license-check-and-add": "^2.3.6",
    "npm-run-all": "^4.1.3",
    "pouchdb-server": "^4.1.0",
    "ts-node": "^7.0.1",
    "yarn": "^1.10.1"
  }
}
